pipeline {
	
	archiveArtifacts allowEmptyArchive: true,
    artifacts: 'cicd/CICD/target/screenshots/**'
    
	agent any
	
	tools {
        jdk 'java21'
        maven 'maven'
    }
    
    parameters {
	    booleanParam(
	        name: 'RUN_UI_TESTS',
	        defaultValue: true,
	        description: 'Run Selenium UI tests'
	    )
	}
	
	stages{
		
		stage('Checkout') {
			
			steps {
				checkout scm
			}
			
		}
		
		stage('Build and Test') {
			steps {
				dir('cicd/CICD') {
					sh 'mvn clean test package'
				}
				
			}
		}
		
		stage('UI Tests (Selenium)') {
    		when {
        		expression { return params.RUN_UI_TESTS }
    		}	
    		steps {
				dir('cicd/CICD') {
        			sh 'mvn -B verify -DskipUnitTests=true'
        		}
    		}
		}
		
		
	}
	
	post {
	    always {
	        junit allowEmptyResults: true, testResults: '''
	            cicd/CICD/target/surefire-reports/*.xml,
	            cicd/CICD/target/failsafe-reports/*.xml
	        '''
	    }
	}
	
}